interface Coordinate {
    x: f32;
    y: f32;
}

interface Locations {
    home: Coordinate;
    work: Coordinate;
}

interface NullableCoordinate {
    x: f32?;
    y: f32?;
}

interface LocationsMaybeNoWork {
    home: NullableCoordinate;
    work: NullableCoordinate?;
}
